char	**ft_fill(char const **s, char c)
{
	char	**res;

	if (!(*s))
		return (NULL);
	res = (char**)malloc(sizeof(char*) * (1 + ft_count(*s, c)));
	if (!res)
		return (NULL);
	while (**s == c && **s)
		(*s)++;
	return (res);
}
